# Shared Code

Put code here only when it is genuinely shared by more than one app.

Examples:

- SQL/config helpers
- CSV utilities
- common environment helpers
- response formatting helpers

Avoid moving code here too early. Clear app boundaries matter more than deduplication at the start.
